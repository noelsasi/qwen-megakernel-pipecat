# Install with: pip install -r requirements.txt
# GPU: CUDA 12.8+, RTX 5090 — install torch FIRST with:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128

# Core inference
# qwen3_tts architecture is not in any released transformers version yet — must install from source
git+https://github.com/huggingface/transformers.git
huggingface_hub
accelerate

# Pipecat pipeline
pipecat-ai[silero,google]>=0.0.50

# FastAPI server
fastapi
uvicorn[standard]
websockets

# Audio
soundfile
numpy

# STT options (pick one at runtime, both listed for flexibility)
openai-whisper
deepgram-sdk

# Build tooling (megakernel)
pybind11

# Dev / benchmarking
nvitop
